---

kind_replication:
  kind: ReplicationController
  apiVersion: v1

kind_service:
  kind: Service
  apiVersion: v1

kind_serviceaccount:
  kind: ServiceAccount
  apiVersion: v1

metadata:
  metadata:
    labels:
      app: "%%{::}{application}-%%{::}{environment}"
    name: "%%{::}{application}-%%{::}{environment}"
